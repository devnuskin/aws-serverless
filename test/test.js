const assert = require('assert');
const config = require("config");

describe('CreateGetItem', function() {
    this.timeout(5000);
    it('should attempt to create an Item and Retrieve an Item', function(done) {
        config.get("NS_SERVICE")

            assert(true, true);
            done();

    });
});